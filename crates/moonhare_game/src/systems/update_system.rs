#[derive(Debug)]
pub struct UpdateSystem;

impl Default for UpdateSystem {
    fn default() -> Self {
        Self { 
        }
    }
}

impl UpdateSystem {
    pub(crate) fn update(&self) {
        
    }
}